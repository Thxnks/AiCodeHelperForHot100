# 字母异位词分组

## 题目摘要
将互为字母异位词的字符串分组输出。

## 核心思路
构造“标准键”（排序串或 26 计数签名），同键归并。

## 为什么这样做
异位词本质是字符频次一致，标准化后可直接哈希分组。

## 常见错误
- 键设计冲突（比如直接拼数字无分隔）。
- 忘记处理空字符串。

## 复杂度分析
常见排序键方案为 O(n * k log k)。

## 类似题迁移
- 有效的字母异位词
- 同构字符串

## 面试追问
- 如何优化到 O(n * k)？
- 字符集不是小写字母时怎么办？
