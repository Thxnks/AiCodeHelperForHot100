# 二叉树的层序遍历

## 题目摘要
按层返回二叉树每层节点值。

## 核心思路
队列 BFS，每轮先记录当前层节点数 `size`，循环弹出 `size` 个节点。

## 为什么这样做
队列天然符合“先入先出”的层级扩展顺序。

## 常见错误
- 遍历当前层时没有固定 size。
- 子节点入队顺序不一致导致输出混乱。

## 复杂度分析
时间 O(n)，空间 O(n)。

## 类似题迁移
- 二叉树锯齿层序遍历
- 二叉树右视图

## 面试追问
- DFS 也能做层序吗？
- 如果节点数量很大如何做流式输出？
