# 两数之和

## 题目摘要
给定整数数组和目标值，返回两数之和等于目标值的下标。

## 核心思路
一次遍历 + 哈希表。遍历到 `x` 时查 `target - x` 是否已出现。

## 为什么这样做
把“找另一半”从 O(n) 降到 O(1) 均摊查询，整体降为 O(n)。

## 常见错误
- 先插入后查询导致同一元素被重复使用。
- 返回数值而不是下标。

## 复杂度分析
时间 O(n)，空间 O(n)。

## 类似题迁移
- 两数之和 II（有序数组）
- 四数之和

## 面试追问
- 如果数组流式输入怎么办？
- 如果要返回所有不重复答案怎么改？
